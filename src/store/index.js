const state = {
  articlePage: 1
}

const getters = {
}

const actions = {
}

const mutations = {
  setArticlePage(state, payload) {
    state.articlePage = payload
  }
}

export default {
  state,
  getters,
  actions,
  mutations
}